const DATABASE_NAME = '@gameplay'

//@pp:collection

const COLLECTION_USER = `${DATABASE_NAME}:users`
const COLLECTION_APPOINTMENTS = `${DATABASE_NAME}:appointments`

export { COLLECTION_APPOINTMENTS, COLLECTION_USER }
